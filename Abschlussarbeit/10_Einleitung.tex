%!TeX root=00_Abschlussarbeit.tex

\chapter{Einleitung}

Diese Arbeit beschäftigt sich mit Echtzeiterkennung von Haushaltsgegenständen.

\begin{lstlisting}[language=Python, captionpos=b, caption=Beispiel, label={lst:beispiel}]
def hallo_welt():
    print("Hallo Welt! \n Masterarbeit in LaTeX.")
\end{lstlisting}



\begin{lstlisting}[language=Python, captionpos=b, caption=Beispiel, label={lst:beispiel1}]
def hallo_welt():
    print("Das ist ein weiteres Beispiel für einen Quellcode in Python.")
\end{lstlisting}


Dies wurde bereits in \cite[S. xx]{redmon2016you} untersucht.
Es steht auch in den Quellcodes \ref{lst:beispiel} und \ref{lst:beispiel1}.